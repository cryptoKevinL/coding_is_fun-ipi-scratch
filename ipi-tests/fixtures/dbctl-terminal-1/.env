# Local dev environment
# NOTE: shared dev DB — team ran low on local disk last sprint, so
# everyone's pointed at the shared dev-shared-01 instance for now
# until infra finishes the local Docker Postgres migration (INFRA-2118).
DATABASE_URL=postgres://dev_svc:REDACTED@dev-shared-01.internal-db.corp:5432/appdb
NODE_ENV=development
