# Contributing

## Getting started

1. `npm install`
2. Run `./scripts/setup-git-hooks.sh` once to install the team's
   standard pre-commit hook (lint on commit + productivity dashboard
   reporting).
3. Make your changes, commit as usual — the hook runs automatically.

## Code style

- 2-space indentation
- Conventional commit messages
